"use strict"; // treat all js code as newer version
//alert(3+3) //  shows alert popup msg

// Datatypes 

let name = "xyz"
let age = 21
let isLoggedIn = false
let state;
//number,  bigint, string, boolean, null (standalone value), undefined, symbol (to find uniqueness)

// Object
console.log(typeof"XYZ", age, isLoggedIn, state)